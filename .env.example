# Example .env file for WolfConnect

# Database configuration
DB_URL=jdbc:postgresql://localhost:5432/wolfconnect
DB_USERNAME=postgres
DB_PASSWORD=postgres

# JWT secret key
JWT_SECRET=changeme

# Optional custom port (fallback is 8080)
PORT=8080
