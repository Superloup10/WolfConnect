# Example .env file for WolfConnect

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=wolfconnect
DB_USERNAME=postgres
DB_PASSWORD=postgres

# JWT secret key
JWT_SECRET=changeme

# Server configuration
PORT=8080
