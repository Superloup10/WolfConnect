# ===============================
# WolfConnect - Variables d'environnement
# ===============================

# --- CONFIGURATION BASE DE DONNÉES (PostgreSQL) ---

# Pour le réseau Docker Compose (à l'intérieur des conteneurs/app)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=wolfconnect-dev
DB_NAME_TEST=wolfconnect-test

# Credentials de la base
DB_POSTGRES_PASSWORD=postgres
DB_MASTER_USERNAME=master
DB_MASTER_PASSWORD=master
DB_APP_USERNAME=app
DB_APP_PASSWORD=app

# Pour l'extérieur (host local, CI, port mapping, accès direct scripts/outils)
DB_HOST_EXT=localhost
DB_PORT_EXT=5443

# --- CLÉ SECRÈTE JWT ---
JWT_SECRET=changeme

# --- CONFIGURATION SERVEUR APP ---
APP_PORT=8080           # Port à l'intérieur du conteneur/service
APP_PORT_EXT=8081       # Port host exposé / forwardé (modifiable selon dispo locale)

# ===============================
# Consignes :
# - Copier ce fichier en .env à la racine du projet.
# - Adapter les valeurs *_EXT si vos ports sont déjà utilisés/localement.
# - Modifier JWT_SECRET en environnement réel !
# ===============================
